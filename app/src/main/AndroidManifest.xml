<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.datingapp.amour">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- For Android 10 and below -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- For Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Amour"
        android:theme="@style/Theme.Amour">

        <!-- Add your Google Maps API Key here -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB9cz7jLj4fHIzmpk9CvV_ck8j7cHNF-OU" />

        <!-- Google Sign-In -->
        <meta-data
            android:name="com.google.android.gms.auth.api.signin.SkipGoogleServicesCheck"
            android:value="true" />

        <!-- Reorder activities - launcher activity should be first -->
        <activity
            android:name=".WelcomeSignupActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" />
        <activity
            android:name=".ProfileSetupActivity"
            android:exported="false" />
        <activity android:name=".LocationsMapActivity" />
        <activity android:name=".MatchBrowsingActivity" />
        <activity android:name=".NotificationsSettingsActivity" />
        <activity android:name=".MessagingActivity" />
        <activity android:name=".ProfileViewActivity" />
        <activity android:name=".PhoneAuthActivity" />

    </application>
</manifest>